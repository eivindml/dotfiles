/* jshint newcap:false */
function VNode() {}

VNode.prototype = {
    bg_: function (finalChildCount) {
        this.br_ = finalChildCount;
        this.bs_ = 0;
        this.bk_ = null;
        this.bt_ = null;
        this.bh_ = null;
        this.bi_ = null;
    },

    _a_: null,

    get at_() {
        var firstChild = this.bk_;

        if (firstChild && firstChild.bj_) {
            var nestedFirstChild = firstChild.at_;
            // The first child is a DocumentFragment node.
            // If the DocumentFragment node has a first child then we will return that.
            // Otherwise, the DocumentFragment node is not *really* the first child and
            // we need to skip to its next sibling
            return nestedFirstChild || firstChild.ar_;
        }

        return firstChild;
    },

    get ar_() {
        var nextSibling = this.bi_;

        if (nextSibling) {
            if (nextSibling.bj_) {
                var firstChild = nextSibling.at_;
                return firstChild || nextSibling.ar_;
            }
        } else {
            var parentNode = this.bh_;
            if (parentNode && parentNode.bj_) {
                return parentNode.ar_;
            }
        }

        return nextSibling;
    },

    aX_: function (child) {
        this.bs_++;

        if (this.bo_ === true) {
            if (child.bu_) {
                var childValue = child.aw_;
                this.bn_ = (this.bn_ || '') + childValue;
            } else {
                throw TypeError();
            }
        } else {
            var lastChild = this.bt_;

            child.bh_ = this;

            if (lastChild) {
                lastChild.bi_ = child;
            } else {
                this.bk_ = child;
            }

            this.bt_ = child;
        }

        return child;
    },

    bp_: function finishChild() {
        if (this.bs_ === this.br_ && this.bh_) {
            return this.bh_.bp_();
        } else {
            return this;
        }
    }

    // ,toJSON: function() {
    //     var clone = Object.assign({
    //         nodeType: this.nodeType
    //     }, this);
    //
    //     for (var k in clone) {
    //         if (k.startsWith('_')) {
    //             delete clone[k];
    //         }
    //     }
    //     delete clone._nextSibling;
    //     delete clone._lastChild;
    //     delete clone.parentNode;
    //     return clone;
    // }
};

module.exports = VNode;