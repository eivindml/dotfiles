var EventEmitter = require('events-light');
var vdom = require('./vdom');
var VElement = vdom.al_;
var VDocumentFragment = vdom.aG_;
var VComment = vdom.aH_;
var VText = vdom.aI_;
var VComponent = vdom.aJ_;
var virtualizeHTML = vdom.aK_;
var RenderResult = require('../RenderResult');
var defaultDocument = vdom.aL_;
var morphdom = require('../../morphdom');

var EVENT_UPDATE = 'update';
var EVENT_FINISH = 'finish';

function State(tree) {

    this.aM_ = new EventEmitter();
    this.aN_ = tree;
    this.aO_ = false;
}

function AsyncVDOMBuilder(globalData, parentNode, parentOut) {
    if (!parentNode) {
        parentNode = new VDocumentFragment();
    }

    var state;

    if (parentOut) {
        state = parentOut.g_;
    } else {
        state = new State(parentNode);
    }

    this.aP_ = 1;
    this.aQ_ = 0;
    this.aR_ = null;
    this.aS_ = parentOut;

    this.data = {};
    this.g_ = state;
    this.ac_ = parentNode;
    this.global = globalData || {};
    this.aT_ = [parentNode];
    this.aU_ = false;
    this.aV_ = undefined;
    this._r_ = null;

    this._Y_ = null;
    this.a__ = null;
    this._Z_ = null;
}

var proto = AsyncVDOMBuilder.prototype = {
    aC_: true,
    v_: defaultDocument,

    bc: function (component) {
        var vComponent = new VComponent(component);
        return this.aW_(vComponent, 0, true);
    },

    ab_: function (component) {
        var vComponent = new VComponent(component, true);
        this.aW_(vComponent, 0);
    },

    aW_: function (child, childCount, pushToStack) {
        this.ac_.aX_(child);
        if (pushToStack === true) {
            this.aT_.push(child);
            this.ac_ = child;
        }
        return childCount === 0 ? this : child;
    },

    element: function (tagName, attrs, key, component, childCount, flags, props) {
        var element = new VElement(tagName, attrs, key, component, childCount, flags, props);
        return this.aW_(element, childCount);
    },

    aY_: function (tagName, attrs, key, component, childCount, flags, props) {
        var element = VElement.aZ_(tagName, attrs, key, component, childCount, flags, props);
        return this.aW_(element, childCount);
    },

    n: function (node, component) {
        // NOTE: We do a shallow clone since we assume the node is being reused
        //       and a node can only have one parent node.
        var clone = node.b__();
        this.node(clone);
        clone._a_ = component;

        return this;
    },

    node: function (node) {
        this.ac_.aX_(node);
        return this;
    },

    text: function (text) {
        var type = typeof text;

        if (type != 'string') {
            if (text == null) {
                return;
            } else if (type === 'object') {
                if (text.toHTML) {
                    return this.h(text.toHTML());
                }
            }

            text = text.toString();
        }

        this.ac_.aX_(new VText(text));
        return this;
    },

    comment: function (comment) {
        return this.node(new VComment(comment));
    },

    html: function (html) {
        if (html != null) {
            var vdomNode = virtualizeHTML(html, this.v_ || document);
            this.node(vdomNode);
        }

        return this;
    },

    beginElement: function (tagName, attrs, key, component, childCount, flags, props) {
        var element = new VElement(tagName, attrs, key, component, childCount, flags, props);
        this.aW_(element, childCount, true);
        return this;
    },

    ba_: function (tagName, attrs, key, component, childCount, flags, props) {
        var element = VElement.aZ_(tagName, attrs, key, component, childCount, flags, props);
        this.aW_(element, childCount, true);
        return this;
    },

    endElement: function () {
        var stack = this.aT_;
        stack.pop();
        this.ac_ = stack[stack.length - 1];
    },

    end: function () {
        this.ac_ = undefined;

        var remaining = --this.aP_;
        var parentOut = this.aS_;

        if (remaining === 0) {
            if (parentOut) {
                parentOut.bb_();
            } else {
                this.bc_();
            }
        } else if (remaining - this.aQ_ === 0) {
            this.bd_();
        }

        return this;
    },

    bb_: function () {
        var remaining = --this.aP_;

        if (remaining === 0) {
            var parentOut = this.aS_;
            if (parentOut) {
                parentOut.bb_();
            } else {
                this.bc_();
            }
        } else if (remaining - this.aQ_ === 0) {
            this.bd_();
        }
    },

    bc_: function () {
        var state = this.g_;
        state.aO_ = true;
        state.aM_.emit(EVENT_FINISH, this.aD_());
    },

    bd_: function () {
        var lastArray = this._last;

        var i = 0;

        function next() {
            if (i === lastArray.length) {
                return;
            }
            var lastCallback = lastArray[i++];
            lastCallback(next);

            if (!lastCallback.length) {
                next();
            }
        }

        next();
    },

    error: function (e) {
        try {
            this.emit('error', e);
        } finally {
            // If there is no listener for the error event then it will
            // throw a new Error here. In order to ensure that the async fragment
            // is still properly ended we need to put the end() in a `finally`
            // block
            this.end();
        }

        return this;
    },

    beginAsync: function (options) {
        if (this.aU_) {
            throw Error('Tried to render async while in sync mode. Note: Client side await is not currently supported in re-renders (Issue: #942).');
        }

        var state = this.g_;

        if (options) {
            if (options.last) {
                this.aQ_++;
            }
        }

        this.aP_++;

        var documentFragment = this.ac_.be_();
        var asyncOut = new AsyncVDOMBuilder(this.global, documentFragment, this);

        state.aM_.emit('beginAsync', {
            out: asyncOut,
            parentOut: this
        });

        return asyncOut;
    },

    createOut: function () {
        return new AsyncVDOMBuilder(this.global);
    },

    flush: function () {
        var events = this.g_.aM_;

        if (events.listenerCount(EVENT_UPDATE)) {
            events.emit(EVENT_UPDATE, new RenderResult(this));
        }
    },

    S_: function () {
        return this.g_.aN_;
    },

    aD_: function () {
        return this.bf_ || (this.bf_ = new RenderResult(this));
    },

    on: function (event, callback) {
        var state = this.g_;

        if (event === EVENT_FINISH && state.aO_) {
            callback(this.aD_());
        } else if (event === 'last') {
            this.onLast(callback);
        } else {
            state.aM_.on(event, callback);
        }

        return this;
    },

    once: function (event, callback) {
        var state = this.g_;

        if (event === EVENT_FINISH && state.aO_) {
            callback(this.aD_());
        } else if (event === 'last') {
            this.onLast(callback);
        } else {
            state.aM_.once(event, callback);
        }

        return this;
    },

    emit: function (type, arg) {
        var events = this.g_.aM_;
        switch (arguments.length) {
            case 1:
                events.emit(type);
                break;
            case 2:
                events.emit(type, arg);
                break;
            default:
                events.emit.apply(events, arguments);
                break;
        }
        return this;
    },

    removeListener: function () {
        var events = this.g_.aM_;
        events.removeListener.apply(events, arguments);
        return this;
    },

    sync: function () {
        this.aU_ = true;
    },

    isSync: function () {
        return this.aU_;
    },

    onLast: function (callback) {
        var lastArray = this._last;

        if (lastArray === undefined) {
            this._last = [callback];
        } else {
            lastArray.push(callback);
        }

        return this;
    },

    aA_: function (doc) {
        var node = this.aV_;
        if (!node) {
            var vdomTree = this.S_();
            // Create the root document fragment node
            doc = doc || this.v_ || document;
            this.aV_ = node = vdomTree.ap_(doc);
            morphdom(node, null, null, vdomTree, doc, this._r_);
        }
        return node;
    },

    toString: function (doc) {
        var docFragment = this.aA_(doc);
        var html = '';

        var child = docFragment.firstChild;
        while (child) {
            var nextSibling = child.nextSibling;
            if (child.nodeType != 1) {
                var container = docFragment.ownerDocument.createElement('div');
                container.appendChild(child.cloneNode());
                html += container.innerHTML;
            } else {
                html += child.outerHTML;
            }

            child = nextSibling;
        }

        return html;
    },

    then: function (fn, fnErr) {
        var out = this;
        var promise = new Promise(function (resolve, reject) {
            out.on('error', reject).on(EVENT_FINISH, function (result) {
                resolve(result);
            });
        });

        return Promise.resolve(promise).then(fn, fnErr);
    },

    catch: function (fnErr) {
        return this.then(undefined, fnErr);
    },

    isVDOM: true,

    c: function (componentDef, key, customEvents) {
        this._Y_ = componentDef;
        this.a__ = key;
        this._Z_ = customEvents;
    }
};

proto.e = proto.element;
proto.ed = proto.aY_;
proto.be = proto.beginElement;
proto.bed = proto.ba_;
proto.ee = proto.endElement;
proto.t = proto.text;
proto.h = proto.w = proto.write = proto.html;

module.exports = AsyncVDOMBuilder;