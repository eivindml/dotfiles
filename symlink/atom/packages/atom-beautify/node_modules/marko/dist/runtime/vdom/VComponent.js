var VNode = require('./VNode');
var inherit = require('raptor-util/inherit');

function VComponent(component, preserve) {
    this.bf_(null /* childCount */);
    this._a_ = component;
    this.at_ = preserve;
}

VComponent.prototype = {
    ap_: 2
};

inherit(VComponent, VNode);

module.exports = VComponent;