var VNode = require('./VNode');
var inherit = require('raptor-util/inherit');

function VText(value) {
    this.bf_(-1 /* no children */);
    this.av_ = value;
}

VText.prototype = {
    bt_: true,

    ap_: 3,

    ao_: function (doc) {
        return doc.createTextNode(this.av_);
    },

    aZ_: function () {
        return new VText(this.av_);
    }
};

inherit(VText, VNode);

module.exports = VText;