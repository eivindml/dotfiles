var VNode = require('./VNode');
var inherit = require('raptor-util/inherit');

function VText(value) {
    this.bg_(-1 /* no children */);
    this.aw_ = value;
}

VText.prototype = {
    bu_: true,

    aq_: 3,

    ap_: function (doc) {
        return doc.createTextNode(this.aw_);
    },

    b__: function () {
        return new VText(this.aw_);
    }
};

inherit(VText, VNode);

module.exports = VText;